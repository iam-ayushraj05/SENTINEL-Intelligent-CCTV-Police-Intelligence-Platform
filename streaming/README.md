# Sentinel Streaming

MediaMTX is used as the initial stream gateway.

Responsibilities:
- receive RTSP;
- relay streams;
- expose browser-compatible playback;
- monitor stream availability.

Vendor-specific VMS integration belongs in `backend/app/integrations/`.
