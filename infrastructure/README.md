# Infrastructure

Production deployment will contain:
- Kubernetes manifests;
- ingress/API gateway;
- PostgreSQL HA;
- Redis;
- Kafka;
- GPU inference workers;
- MediaMTX/stream gateways;
- object storage;
- Prometheus/Grafana;
- secrets management.

Keep local development in `docker-compose.yml`.
