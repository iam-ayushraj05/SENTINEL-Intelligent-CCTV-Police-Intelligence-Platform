class CorrelationEngine:
    """Combines AI observations with authorized watchlist/history data."""

    async def process_detection(self, detection: dict) -> list[dict]:
        # Phase 1 foundation: real correlation rules will be implemented next.
        return []
